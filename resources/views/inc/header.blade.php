<div class="header">
    <div class="header_body">
        <a href="{{route('home')}}" class="logo">
            <i class='bx bx-atom logo-img'></i>
            <p class="logo_name">Semka</p>
        </a>
        <div class="panel">
            <span></span>
        </div>
        <div class="header_tab">
            <a href="{{route('home')}}" class="tab">Главная</a>
            <a href="#" class="tab">Меню</a>
            <a href="{{route('reviews')}}" class="tab">Отзывы</a>
        </div>
    </div>
</div>